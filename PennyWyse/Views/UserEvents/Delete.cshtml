@model PennyWyse.Models.UserEvent

@{
    ViewData["Title"] = "Delete";
    string searchId = Context.Request.Query["searchId"].ToString();
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>UserEvent</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.User)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.User.Id)
        </dd class>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Event)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Event.Name)
        </dd class>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="UserEventId" />
        <input type="submit" value="Delete" class="btn btn-danger" asp-route-searchId="@searchId" /> |
        <a asp-action="Index" asp-route-searchId="@searchId">Back to List</a>
    </form>
</div>
